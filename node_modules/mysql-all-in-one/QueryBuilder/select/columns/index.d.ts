import { PreparedStatement } from '../../types';
import { SelectColumns } from './types';
declare const create_columns: (columns?: SelectColumns | undefined, alias?: string | undefined) => PreparedStatement;
export default create_columns;
